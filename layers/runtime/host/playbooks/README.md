# Self-heal playbooks — Faza 2.2 / Faza 7 (minimal)

| Script | Trigger |
|--------|---------|
| `health-gate-fail.sh` | `OnFailure=` of `aions-health.service` |
| `restart-api.sh` | Called by health-gate-fail |
| `restart-mcp.sh` | Called by health-gate-fail |
| `backup-data.sh` | `aions-backup.timer` (daily) |

Units are generated by `install_aions_host.sh` → `runtime/systemd/user/`.
